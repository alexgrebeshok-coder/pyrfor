import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.ceoclaw.mobile",
  appName: "CEOClaw",
  webDir: "mobile-shell-dist",
};

export default config;
