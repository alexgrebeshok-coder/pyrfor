// Finance page is a client component that uses useDemoWorkspaceMode() internally.
// Rendering it under /demo/workspace/finance automatically serves demo data.
export { default } from "@/app/finance/page";
