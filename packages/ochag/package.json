{
  "name": "@ochag/family",
  "version": "0.1.0",
  "description": "Очаг — семейный AI: Memory, Family Calendar, Voice Profile, Vault, Telegram Mini App",
  "license": "Apache-2.0",
  "private": false,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc --build",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "dev": "tsc --build --watch"
  },
  "dependencies": {
    "@ceoclaw/engine": "workspace:*",
    "@ceoclaw/ui": "workspace:*"
  },
  "peerDependencies": {
    "typescript": ">=5.0.0"
  }
}
