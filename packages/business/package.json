{
  "name": "@ceoclaw/business",
  "version": "0.1.0",
  "description": "CEOClaw Business — SMB plugins: EVM, КС-2/3, ГОСТ, 1С OData, Enterprise multi-tenant",
  "license": "BSL-1.1",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc --build",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "dev": "tsc --build --watch"
  },
  "dependencies": {
    "@ceoclaw/engine": "workspace:*",
    "@ceoclaw/ui": "workspace:*"
  },
  "peerDependencies": {
    "typescript": ">=5.0.0"
  }
}
